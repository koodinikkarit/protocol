
import classes from "./"

export default class {
	constructor(context, {
		id
	}) {
		var _id = id;

		Object.defineProperties(this, {
			
		})
	}
	editComputer({
		computerId,
		arttuId	
	}) {
		
	}
}
